# 현재 상태

하네스 기준선 갱신일: **2026-07-22**

## 완료

- Next.js App Router MVP 페이지 5개
- `recommended`, `hold`, `excluded` 세 판정 모델
- 순수 TypeScript 결정론적 규칙 엔진
- 조직 이메일 강제 제외
- 최근 조회 평가와 바이럴 왜곡 감지
- 채널 ID·URL·핸들·별칭·정확한 이름 중복 매칭
- 기존 히스토리 중복과 동일 실행 중복 차단
- 수동 교정의 제외 판정 및 히스토리 반영
- `HistoryRepository`와 브라우저 `localStorage` 구현
- 24개 허구 목 시나리오
- 8개 테스트 파일의 62개 단위·하네스 계약 테스트
- `npm run verify` 통합 검증 명령

## 미구현

- 실제 YouTube 연동과 크리에이터 탐색
- 실제 이메일 수집
- 실시간 시청자 적합성 검증
- 프로덕션 데이터베이스
- 인증과 권한
- 예약 작업과 백그라운드 실행
- 배포 인프라
- 실제 레거시 히스토리 마이그레이션
- AI 기반 탐색 또는 분석

## 현재 제한사항

- 히스토리는 브라우저와 기기에 종속된 `localStorage`에만 저장됩니다.
- 목 근거는 실제 크리에이터 증거가 아닙니다.
- 서버 측 자동 스카우팅을 실행할 수 없습니다.
- 규칙 엔진은 공급된 입력 데이터만 평가합니다.
- 최근 조회 평가기는 공급된 조회수 배열이 이미 비교 가능한 동일 형식 표본이라고 가정하며, 영상별 Shorts·롱폼 형식을 자동 분할하지 않습니다.
- 실제 과거 크리에이터 목록은 가져오지 않았습니다.

현재 안전 기준선과 명령은 [`verification.md`](verification.md)에 기록합니다. 다음 권장 작업은 [`next-task.md`](next-task.md)의 H1 UI 스모크 테스트입니다.
